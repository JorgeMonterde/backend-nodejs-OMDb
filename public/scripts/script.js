/* const searchForm = document.querySelector("#search_form");
if (searchForm){
    searchForm.addEventListener("submit", (event)=>{
        const title = event.target.title.value;
        document.location.href = "http://localhost:3000/film/:title"
    })
} */